# Athena runtime dependencies.
# Pinned to the versions currently in use; adjust as needed.

# Local GGUF model inference — OPTIONAL.
#
# Athena's default "llamaserver" provider runs models through the prebuilt
# llama.cpp binaries that setup.bat downloads, so this package is NOT needed
# and setup.bat deliberately skips it.
#
# It has no prebuilt Windows wheels, so installing it compiles llama.cpp from
# source and requires Visual Studio Build Tools. Only install it if you
# specifically want in-process inference via the "llamacpp" provider —
# docs/SETUP.md explains how.
#
# llama-cpp-python==0.3.32

# Hardware detection (CPU/RAM/GPU) and system snapshots
psutil==7.2.2
py-cpuinfo==9.0.0

# HTTP (LM Studio provider, web search)
requests==2.34.2

# Web search tool
duckduckgo-search==8.1.1

# Book reading mode (/book) — PDF text extraction
pypdf==6.14.2

# Serve mode (/serve) — OpenAI-compatible API for external clients
fastapi==0.138.1
uvicorn==0.49.0
